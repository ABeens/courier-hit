// @ts-check
import { defineConfig } from 'astro/config';

// Sitio 100% estático por ahora (landing público). Cuando llegue el portal
// (Opción B) añadiremos la integración de React aquí para las islas.
export default defineConfig({
  site: 'https://hsgloballtd.com',
});
